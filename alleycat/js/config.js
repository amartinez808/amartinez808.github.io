// Optional deployment overrides. Keep this file small so a host can switch
// providers without touching application code.
window.ZIGZAG_CONFIG = window.ZIGZAG_CONFIG || {
  // nominatimBase: "https://your-geocoder.example",
  // overpassEndpoint: "https://your-overpass.example/api/interpreter",
};

